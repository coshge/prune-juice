target/
*.db
*.db-wal
*.db-shm
.DS_Store
app/PruneJuice/.build/
app/PruneJuice/dist/

# Release signing keys. The secret halves cannot be rotated for copies already
# installed, so a key reaching a public repository is not a mistake with a fix
# — see RELEASING.md. This does not stop `git add -f`; it stops `git add .`.
*.key
*.pub
sparkle.key
